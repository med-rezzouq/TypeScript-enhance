//tuple is when we can define strictly number and type of elements in array

let person: [string, string, number] = ['john', 'jeni', 2020]

// so if we try to push another 4th element ts compiler will complain
//also if we try to put the number as first element it will complain